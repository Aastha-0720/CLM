# Scripts

This folder contains one-off and maintenance scripts that are not part of the runtime application.

- `scripts/frontend/`: frontend helper scripts
- `scripts/backend/`: backend debug, test, and maintenance scripts

These scripts are optional utilities and are not required to run the app services.
